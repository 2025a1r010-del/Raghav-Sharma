#include<stdio.h>
#include<math.h>
int main(){
    int x, y;
    printf("Enter x: ");
    scanf("%i", &x);
    printf("Enter y: ");
    scanf("%i", &y);
    if(x>y)
        printf("x is larger than y\n");
    else
    printf("y is larger than x\n");

    return 0;
}
