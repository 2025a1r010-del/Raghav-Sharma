#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num1, num2, hcf;
    scanf("%d", &num1);
    scanf("%d", &num2);

    int a = num1, b = num2;
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }

    hcf = a;

    printf("%d\n", hcf);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
