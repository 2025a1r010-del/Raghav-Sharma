#include<stdio.h>
#include<math.h>
int main(){
    int sum=0, p=0, bin;
    scanf("%d", &bin);
    int temp=bin;
while(temp!=0){
    int rem=temp%10;
    sum=sum+rem*pow(2, p);
    p++;
    temp=temp/10;
}
printf("%d ", sum);
return 0;
}
